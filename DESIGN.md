# Kibitz — Design System

> **Kibitz** (v.) 체스에서 옆에 서서 지켜보며 훈수를 두는 것.
> 에이전트 실행을 지켜보고, 어디서 어긋났는지 짚어주는 오픈소스 옵저버빌리티.

## 0. 설계 근거 — 왜 span 트리를 버리는가

Langfuse · Arize Phoenix · Honeycomb · Grafana를 전부 열어본 결과, 넷이 똑같은 두 가지를 그린다:

1. **span 워터폴** — `POST /api/questions/{id}/answer` → `Ask LLM for Response` → `chat with AI` → `HTTP POST`
2. **노드-엣지 다이어그램** — In → Orchestrator → LLM Call → Synthesizer → Out

둘 다 **프로그램의 콜그래프**다. 프로파일러의 산물이지 에이전트의 산물이 아니다.
그런데 사람이 에이전트를 디버깅할 때 던지는 질문은 "어느 함수가 느렸나"가 아니라 **"얘가 왜 저렇게 판단했나"** 이다.

Phoenix의 알려진 결함이 이걸 증명한다 — *span 수천 개짜리 에이전트 런에서 트리는 무너지고, 정작 보고 싶은 건 대화다.*
Langfuse의 결함도 같은 뿌리다 — *실패 유형(failure mode)이 추적 가능한 엔티티로 존재하지 않는다.*

**Kibitz의 1차 객체는 span이 아니라 결정(Decision)이다.**

### 참조 도메인: 체스 분석판

lichess 분석판은 정확히 같은 문제를 이미 풀었다. 80수짜리 대국을 초보도 3초 만에 "여기서 망했구나"를 본다. 훔쳐올 장치는 넷:

| lichess | Kibitz |
|---|---|
| 하단 평가 곡선 + 국면 라벨(오프닝/미들/엔드) | **Run Ribbon** — 런 전체를 한 스트립으로. 국면 = 계획/수집/추론/전달 |
| 수 목록 안에 인라인으로 꽂히는 `Inaccuracy. e6 was best.` | **Inline Critique** — 실수 지점 그 자리에서, 대안까지 |
| 부정확 3 · 실수 3 · 블런더 2 · 정확도 89% | **Run Scorecard** — 런 하나의 품질 점수 |
| 탭: 컴퓨터 분석 / 이동 시간 / 점수판 | **Lens 탭** — 같은 척추에 판단/지연/비용/컨텍스트를 갈아끼움 |

Perfetto의 상단 압축 미니맵이 Run Ribbon 개념을 검증한다. Warp의 블록 하단 메타 칩이 Decision Card의 칩 행을 검증한다.

---

## 1. Visual Theme & Atmosphere

**관제실이 아니라 분석실.**

기존 옵저버빌리티 툴은 전부 "관제실"이다 — 빽빽한 표, 경보색, 밀도로 신뢰를 사려 한다. Kibitz는 반대로 간다. 사람이 한 런을 붙잡고 30분씩 들여다보는 도구이므로, **읽는 것이 피곤하지 않아야 한다.**

다크 네이티브다. 오래 보는 화면이고, 상태색(성공/재시도/실패/사람개입)이 어두운 바탕에서만 제대로 노래한다. 그러나 Linear식 "정밀 기계"의 차가움도, Langfuse식 대시보드 밀도도 취하지 않는다. 대신 **`aside`의 공간 감각을 다크로 이식한다** — 큰 수직 리듬, 초대형 타이포, 본문의 2톤 처리, 여백을 두려워하지 않는 태도.

핵심 원칙 하나: **기본은 사람의 말, 상세는 기계의 말.** 지금 모든 툴이 정반대다. 카드의 기본 상태는 "쿼리가 너무 좁았다. 넓혀서 다시 찾는다" 같은 한 문장이고, 펼쳐야 raw JSON이 나온다.

**Key Characteristics**
- 다크 네이티브: `#0A0B0D` 앱 바탕, `#101216` 패널, `#171A1F` 융기면
- 색은 오직 의미로만 쓴다. 장식 그라디언트 없음. 상태 5색 + 액센트 1색이 전부
- 액센트는 스카이(`#6FC7FF`) 하나. 선택·포커스·인터랙션에만
- 반투명 흰 보더 (`rgba(255,255,255,0.06~0.10)`) — 구조는 만들되 소음은 안 만든다
- 본문 2톤: 리드 문장 `#F2F4F7`, 이어지는 설명 `#9BA3AF`
- 모노는 기계의 말에만 (툴 호출, ID, JSON). 사람의 말에는 절대 안 씀

---

## 2. Color Palette & Roles

### Surfaces
| 토큰 | 값 | 역할 |
|---|---|---|
| `--ink-900` | `#0A0B0D` | 앱 바탕. 거의 검정, 미세한 청색 언더톤 |
| `--ink-800` | `#101216` | 패널·사이드바 |
| `--ink-750` | `#141720` | 리본 트랙 바탕 |
| `--ink-700` | `#171A1F` | 카드·융기면 |
| `--ink-600` | `#1E2229` | hover·선택 배경 |

### Lines
| 토큰 | 값 | 역할 |
|---|---|---|
| `--line-subtle` | `rgba(255,255,255,0.06)` | 기본 구획선 |
| `--line` | `rgba(255,255,255,0.10)` | 카드 보더 |
| `--line-strong` | `rgba(255,255,255,0.16)` | 선택된 요소 |

### Text
| 토큰 | 값 | 역할 |
|---|---|---|
| `--text` | `#F2F4F7` | 판단 문장, 제목 |
| `--text-2` | `#9BA3AF` | 이어지는 설명, 부제 |
| `--text-3` | `#646C78` | 메타데이터, 타임스탬프 |

### Semantic — 결정의 결과
색은 **상태를 말하기 위해서만** 쓴다. 이 5색 외의 색은 이 제품에 없다.

| 토큰 | 값 | 의미 |
|---|---|---|
| `--ok` | `#4ADE80` | 의도한 결과를 얻음 |
| `--retry` | `#FBBF24` | 결과 부족 → 재시도 |
| `--fail` | `#FB7185` | 실패·예외 |
| `--human` | `#C084FC` | 사람이 개입함 |
| `--blunder` | `#FF8A5B` | 자동 탐지된 실수 |
| `--accent` | `#6FC7FF` | 선택·포커스 (상태 아님) |

> **Do**: 상태색은 리본 셀, 거터 글리프, 결과 칩에만.
> **Don't**: 상태색으로 배경을 채우지 않는다. 카드 전체가 빨개지면 읽을 수 없다.

---

## 3. Typography

### Family
- **Sans**: `ui-sans-serif, -apple-system, "SF Pro Display", "Inter Variable", Inter, system-ui`
- **Mono**: `ui-monospace, "SF Mono", "JetBrains Mono", Menlo, monospace`

### Hierarchy
| 역할 | 크기 | 두께 | 자간 | 용도 |
|---|---|---|---|---|
| Display | 44px | 600 | -1.2px | 런 제목 |
| Title | 28px | 600 | -0.6px | 섹션 |
| Score | 34px | 600 | -0.8px | 스코어카드 숫자 |
| Decision | 16px | 500 | -0.1px | **판단 문장 — 이 제품의 주인공** |
| Body | 14px | 400 | 0 | 설명 |
| Meta | 12px | 500 | 0 | 칩, 라벨 |
| Micro | 11px | 500 | 0.3px | 오버라인 (대문자) |
| Mono | 13px | 400 | 0 | 툴 호출, ID |
| Mono S | 11.5px | 400 | 0 | JSON |

### 원칙
- **판단 문장이 위계의 정점이다.** span 이름이 아니라 판단이 가장 크고 밝다
- 디스플레이 크기에서만 음의 자간. 14px 이하는 건드리지 않는다
- 두께는 400 / 500 / 600 셋뿐. 그 이상 쓰면 위계가 무너진다
- 숫자는 전부 `font-variant-numeric: tabular-nums`

---

## 4. Components

### 4.1 Run Ribbon — 시그니처
런 전체를 가로 스트립 하나로 압축한다. span이 수천 개여도 이 스트립은 항상 한 화면.

- **셀**: 턴 하나 = 셀 하나. 폭 ∝ 소요시간(min 10px), 채움색 = 상태
- **확신도 곡선**: 셀 아래 SVG area chart. 에이전트가 스스로 매긴 확신도
- **블런더 마커**: 셀 위 작은 삼각형(`--blunder`). 클릭하면 해당 결정으로 점프
- **높이**: 트랙 44px + 곡선 40px
- hover → 툴팁, click → 선택 + 카드로 스크롤
- **Lens에 따라 채움색이 바뀐다**: 판단=상태색 / 지연=열지도 / 비용=열지도 / 컨텍스트=압박도

> 이 컴포넌트가 Phoenix의 붕괴 지점(span 수천 개)을 정면 해결한다.

### 4.2 Decision Card
```
◆  쿼리가 너무 좁았다. 범위를 넓혀 다시 찾는다          2.1s  1.2K  $0.004
   search(q="매출 2026 Q3")  →  3건                              ▸
```
- **거터 글리프**: `●` 사용자 / `◆` 판단 / `⚑` 사람 개입 / `✓` 완료. 색 = 상태
- **1행**: 판단 문장 (16px/500, `--text`) + 우측 메타 칩
- **2행**: 툴 호출 (mono 13px, `--text-2`) + 결과 칩
- **접힘이 기본.** 펼치면 raw JSON (mono 11.5px)
- 선택 시: 배경 `--ink-600`, 좌측 2px 액센트 바
- 카드 배경은 기본 투명. 카드가 상자로 안 보이게 — 읽기 흐름이 끊긴다

### 4.3 Inline Critique — 시그니처
lichess의 `Inaccuracy. e6 was best.` 를 그대로 가져온다. **실수 지점 그 자리에** 끼워 넣는다.

```
┌ ⚠ 같은 도구를 같은 인자로 다시 불렀다
│   직전 호출과 인자가 동일하다. 결과가 바뀔 이유가 없었다.
│   → 쿼리를 넓히거나 다른 도구로 갔어야 한다.        [ 3번 턴 보기 ]
```
- 배경 `rgba(255,138,91,0.07)`, 좌측 2px `--blunder`
- 3부 구조 고정: **무슨 일이 있었나 / 왜 문제인가 / 대신 뭘 했어야 하나**
- 이게 Langfuse에 없는 "failure mode를 엔티티로 다루기"의 UI 형태다

### 4.4 Context X-ray — 시그니처
각 LLM 호출 시점에 **컨텍스트 창에 실제로 뭐가 들어있었나**를 누적 띠로.

```
시스템 6% │ 스킬 11% │ ████ 툴 정의 47% ████ │ 히스토리 28% │ 검색결과 8%
⚠ 등록된 툴 41개 중 이 런에서 실제 호출된 건 3개
```
어떤 경쟁 툴에도 없다. 그리고 이건 실측된 통증과 직결된다 — GitHub MCP 서버 하나가 93개 툴 정의에 55K 토큰을 먹고, MCP는 동일 작업 CLI 대비 4~32× 토큰을 쓴다.

### 4.5 Run Scorecard
lichess 하단 통계의 이식.
```
정확도 71%      블런더 3      낭비된 턴 2      사람 개입 1
```
- 숫자 34px/600 tabular, 라벨 11px 대문자 `--text-3`
- 정확도 = (의도한 결과를 얻은 턴) / (전체 턴)

### 4.6 Lens Tabs
`판단 · 지연 · 비용 · 컨텍스트`. 밑줄형 탭, 활성 시 `--text` + 2px 액센트 밑줄.
같은 척추(리본+카드)에 다른 척도를 끼운다.

---

## 5. Layout

```
┌ topbar 52px ─────────────────────────────────────────────┐
├ run header — 제목 + 스코어카드 ──────────────────────────┤
├ RUN RIBBON (full-bleed) ─────────────────────────────────┤
├ lens tabs ───────────────────────────────────────────────┤
├─ NARRATIVE  minmax(0,1fr) ──┬─ EVIDENCE  420px (sticky) ─┤
│  결정 카드 세로 스택         │  선택된 결정의 근거        │
│  + 인라인 비평               │  프롬프트 / 응답 / X-ray   │
└──────────────────────────────┴────────────────────────────┘
```
- 최대 폭 1440px, 좌우 32px 패딩
- 서사 열 최대 폭 **760px** — 문장이 주인공이므로 읽기 폭을 지킨다
- 증거 패널 `position: sticky`
- 카드 간 세로 간격 4px (촘촘) / 국면 전환 시 24px (숨 쉬는 곳)

---

## 6. Depth & Elevation

그림자를 거의 안 쓴다. 다크에서 그림자는 진흙이 된다. 깊이는 **표면 밝기**로만 만든다.

`--ink-900` → `--ink-800` → `--ink-700` → `--ink-600` 네 단계.

예외 둘:
- 툴팁/팝오버: `0 8px 24px rgba(0,0,0,0.5)` + `1px solid var(--line)`
- 선택된 리본 셀: `0 0 0 2px var(--accent)` 링

---

## 7. Interaction & Motion

**모션은 인과를 설명할 때만 쓴다.** 장식 애니메이션 없음.

| 동작 | 지속 | 이징 |
|---|---|---|
| hover 색 변화 | 120ms | `ease-out` |
| 카드 펼침 | 180ms | `cubic-bezier(0.32, 0.72, 0, 1)` |
| 리본→카드 스크롤 | 320ms | `cubic-bezier(0.32, 0.72, 0, 1)` |
| 선택 이동 | 140ms | `ease-out` |

- 리본 셀 hover → 대응 카드가 동시에 밝아진다 (양방향 하이라이트). 이 연결이 리본을 신뢰하게 만든다
- **키보드 `←` `→` 로 턴 이동** — 체스 분석판의 수 넘기기 그대로. 이게 핵심 인터랙션이다
- `prefers-reduced-motion` 존중

---

## 8. Do's and Don'ts

| Do | Don't |
|---|---|
| 카드 제목은 에이전트가 **하려던 것**을 사람 말로 | `retriever.invoke` 같은 함수명을 제목으로 |
| raw JSON은 접어두고 요청 시 펼침 | JSON을 기본 노출 |
| 상태색은 글리프·칩·리본 셀에만 | 카드 배경을 상태색으로 |
| 비평은 실수 지점 인라인 | 비평을 별도 탭으로 분리 |
| 숫자는 tabular-nums | 비례 숫자로 표 흔들기 |
| 여백을 남긴다 | 밀도로 신뢰를 사려 함 |

---

## 9. Agent Prompt Guide

이 디자인 시스템으로 화면을 만들 때 에이전트에게 줄 지시:

> 다크 네이티브 UI를 만든다. 바탕 `#0A0B0D`, 패널 `#101216`, 카드 `#171A1F`.
> 텍스트는 `#F2F4F7`(주) / `#9BA3AF`(부) / `#646C78`(메타) 3단.
> 액센트는 `#6FC7FF` 하나뿐이고 선택·포커스에만 쓴다.
> 상태는 `#4ADE80`(성공) `#FBBF24`(재시도) `#FB7185`(실패) `#C084FC`(사람) `#FF8A5B`(실수) 5색이고, 글리프·칩·리본 셀에만 칠한다. 배경에는 절대 안 칠한다.
> 폰트는 시스템 산세리프, 두께 400/500/600만. 숫자는 tabular-nums.
> **화면의 주인공은 "에이전트가 무엇을 하려 했는가"를 서술한 16px/500 한국어 문장이다.**
> 함수 이름·JSON·ID는 모노 폰트로 낮춰서 부차적으로 둔다.
> 보더는 `rgba(255,255,255,0.06~0.10)`. 그림자는 툴팁 외에 쓰지 않는다.
> 여백을 아끼지 않는다. 서사 열은 760px를 넘지 않는다.
